<%= form_for(current_user.friends.find_by(target_user_id: user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unmake friend", class: "btn btn-xs btn-default" %>
<% end %>
